---
category: Primitives
---

# Textarea

The multi-line sibling of `Input`, same border and focus ring, vertically resizable, minimum 80px tall.

```tsx
<Textarea rows={3} placeholder="Note de séance" />
```

Pair with `Field` for a label.
